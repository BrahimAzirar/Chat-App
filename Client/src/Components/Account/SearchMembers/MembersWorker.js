const GetMembers = async (apiUrl) => {
    try {
        
    } catch (error) {
        throw new Error(error.message);
    }
};

self.onmessage = (e) => {
    try {
        
    } catch (error) {
        self.postMessage({ err: error.message });
    }
}